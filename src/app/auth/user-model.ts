export interface LoginModel {
    email: string;
    password: string;
}

export interface RegisterModel {
    username: string;
    email: string;
    password: string;
}

export interface UserModel {
    bio: null
    email: string
    image: null
    token: string
    username: string
}